{% extends '::base.html.twig' %}

{% block body %}
    <ul id="navigation">
        {% for product in products %}
            <li><a href="{{ path('mvs_sample_show', {'id': product.id}) }}">{{ product.name }}</a> </li>
        {% endfor %}
    </ul>
{% endblock %}